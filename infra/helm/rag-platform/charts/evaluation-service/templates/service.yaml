apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-evaluation-service
  labels:
    app: evaluation-service
    release: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: evaluation-service
    role: api
    release: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
