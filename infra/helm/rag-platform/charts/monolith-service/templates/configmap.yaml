apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-monolith-service-config
  labels:
    app: monolith-service
    release: {{ .Release.Name }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
