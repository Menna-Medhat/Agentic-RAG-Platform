apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-monolith-service
  labels:
    app: monolith-service
    release: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: monolith-service
    release: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
