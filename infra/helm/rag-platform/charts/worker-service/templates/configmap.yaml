apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-worker-service-config
  labels:
    app: worker-service
    release: {{ .Release.Name }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
